FROM node:15.8.0-alpine3.10

ARG WORKDIR
ARG CONTAINER_PORT
ARG API_PORT

ENV HOME=/${WORKDIR} \
  LANG=C.UTF-8 \
  TZ=Asia/Tokyo \
  HOST=0.0.0.0 \
  API_PORT=${API_PORT}

WORKDIR ${HOME}

EXPOSE ${CONTAINER_PORT}
